import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { PopoverShowTextPage } from './popover-show-text';

@NgModule({
  declarations: [
    PopoverShowTextPage,
  ],
  imports: [
    IonicPageModule.forChild(PopoverShowTextPage),
  ],
})
export class PopoverShowTextPageModule {}
