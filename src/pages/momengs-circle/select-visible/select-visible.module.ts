import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { SelectVisiblePage } from './select-visible';

@NgModule({
  declarations: [
    SelectVisiblePage,
  ],
  imports: [
    IonicPageModule.forChild(SelectVisiblePage),
  ],
})
export class SelectVisiblePageModule {}
